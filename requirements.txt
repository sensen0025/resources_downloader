# Resource Hub — 核心运行依赖(服务器部署用)
# 个人版核心: 检索/分析/下载 纯标准库 + requests,零重依赖。
# 浏览器 Agent(需登录/反爬站点)需要 playwright + 系统浏览器。
# API 服务需要 fastapi + uvicorn(测试用 httpx)。

requests>=2.31
playwright>=1.44
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27
